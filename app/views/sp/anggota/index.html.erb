<!--<style media="screen">
  .th {
    text-align: center;
  }
</style> -->



<div class="table-responsive">


  <table class="table table-striped table-bordered">
    <a href="/sp/anggota/baru" class="btn btn-primary">Baru</a>
    <thead>
      <tr>
        <th style="text-align:center">Aksi</th>
        <th style="text-align:center">#</th>
        <th style="text-align:center">Nama</th>
        <th style="text-align:center">Tanggal Lahir</th>
        <th style="text-align:center">Alamat</th>
        <th style="text-align:center">Jenis Kelamin</th>


      </tr>
    </thead>
    <tbody>

      <% @datas.each do |f| %>
      <tr>
        <td>
          <a href="<%= url_for action: 'view', anggota_id: f.id %>" class="btn btn-small btn-default">View</a>
          <a href="<%= url_for action: 'edit', anggota_id: f.id %>" class="btn btn-small btn-success">Edit</a>
          <%= link_to 'hapus',url_for(action: :hapus, anggota_id: f.id), method: :delete, data: {confirm: "Apakah Anda yakin ingin menghapus data tersebut?"}, class: 'btn btn-small btn-danger' %>
        </td>
        <td><%= f.id %></td>
        <td><%= f.anggota_nama %></td>
        <td><%= f.anggota_tgllahir %></td>
        <td><%= f.anggota_alamat %></td>
        <td><%= f.anggota_jk %></td>
      </tr>
      <% end %>


    </tbody>
  </table>
  <!-- <div class="row">
    <div class="col">
      <form class="form-inline mr-auto" action="/sp/anggota/search">
        <div class="search-element">
            <input class="form-control" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-primary" type="submit"><i class="ion ion-search"></i></button>
        </div>
        </form>

    </div>

  </div> -->
</div>
